<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unifor.dispmoveis.uniforlabs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Laboratórios de Informática"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BlocoActivity" />
        <activity android:name="pasta.fragments.em.aula.prox.aula.SalaD18Activity" />
        <activity android:name=".LinhaAtributosBlocoActivity" />
        <activity android:name="pasta.fragments.em.aula.prox.aula.SalaM35Activity"></activity>
    </application>

</manifest>